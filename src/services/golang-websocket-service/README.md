# Things yet to add

Database should be updated after connection is created, not before

Database should be updated when a user leaves, the connection ends

Validation that the same userID can't join twice

If the owner disconnects from the websocket then everyone else should disconnect
